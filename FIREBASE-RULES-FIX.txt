Firebase Console - قواعد الأمان المطلوبة
==========================================

⚠️ تحذير مهم جداً:
النظام حالياً ممكن البيانات مش ظاهرة للمستخدم الثاني بسبب قواعد الأمان في Firebase!

📋 خطوات الحل:

1. افتح: https://console.firebase.google.com
2. اختر المشروع: okcomputer-system
3. من القائمة اليسار → اضغط على "Firestore Database"
4. اضغط على تبويب "Rules" (القواعد)
5. احذف كل شيء واستبدله بهذا الكود:

```
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // السماح للجميع بالقراءة والكتابة على جميع المجموعات
    // ⚠️ ملاحظة: هذا مناسب للأنظمة الداخلية الخاصة فقط
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

6. اضغط "Publish" (نشر)

⚠️ ملاحظة أمنية:
- هذه القواعد تسمح لأي شخص عنده رابط النظام بالوصول للبيانات
- إذا تريد أمان أفضل، اكتب لي وراح أسويلك نظام تسجيل دخول

---

🔍 للتحقق من المشكلة الحالية:
افتح Developer Tools (F12) → Console
إذا كان فيه رسالة خطأ مثل:
"Missing or insufficient permissions"
يعني المشكلة من القواعد وتحتاج تطبق الحل فوق.

---

💡 بديل آخر (إذا تحتاج أمان أكثر):
ممكن نسوي نظام باسم مستخدم وكلمة سر بسيطة
اكتبلي إذا تبي هذا الحل
